node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Generated from /cv/ at build time by scripts/print-cv.mjs.
build/cv.pdf

# Local preview of the MEng thesis, pending permission to publish it. Remove
# this line (and the TODO in src/lib/data/experience.ts) once it is cleared.
static/masters-thesis.pdf

# Personal working notes on what the CV still needs. Kept out of the public
# repo deliberately: it names an in-flight job application and open questions
# for supervisors.
CV-TODO.md
